#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    // -- Init AI Client

    // -- User question
    let question = "Why is the sky red? (be concise)";

    // -- Build messages

    // -- Exec Chat Request

    // -- Display response

    Ok(())
}
